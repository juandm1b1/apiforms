# from django.urls import path #Se crea archivo urls.py para cada app, y se llaman desde el urls.py del proyecto
# from app1 import views

# urlpatterns = [                               #Falta crear vistas (y por tanto la app, y por tanto la BD)
#     path('nameform/', views.get_name, name="Name_form"), 
# ] 